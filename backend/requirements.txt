# Core dependencies
requests==2.31.0
python-dotenv==1.0.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# eBay API
ebaysdk==2.2.0

# Web scraping
beautifulsoup4==4.12.2
selenium==4.15.2
webdriver-manager==4.0.1

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# API framework
fastapi==0.104.1
uvicorn==0.24.0
# Required for File()/Form() routes (e.g. inventory bulk-import); without it Lambda fails to import the app.
python-multipart>=0.0.9
# 2.10+ required if nova-act / MCP share this env (pip warns below 2.10.6)
pydantic>=2.10.6,<3

# Task scheduling
apscheduler==3.10.4

# Utilities
python-dateutil==2.8.2

# Amazon Nova (OpenAI-compatible chat + multimodal) — vision retry, dev scrapers
# Needs Python 3.9+ (3.10+ recommended). openai 2.x + jiter may not resolve on 3.8; use one clean venv (see PIPELINE-OPS).
openai>=1.40.0

# Auth (Cognito JWT)
python-jose[cryptography]==3.3.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.27.0
# Fallback DB-API when Python is built without stdlib _sqlite3 (some /usr/local installs); QA in-memory SQLite
pysqlite3-binary==0.5.4.post2

# Configuration
pyyaml==6.0.1
